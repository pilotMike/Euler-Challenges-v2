namespace ScratchPad
//let f arr = arr |> List.map abs // Complete this function
//
////----------------DON'T MODIFY---------------
//
//let input = 
//    stdin.ReadToEnd().Split '\n' 
//    |> Array.map(fun x -> int(x)) 
//    |> Array.toList
//    
//let print_out (data:int list) = List.iter (fun x -> printfn "%A" x) data
//
//print_out (f(input))
//open System
//
//    module Stuff =
//        let stringMingling s1 s2=
////            let rec mingle a b source =
////                match a with
////                | head :: tail -> 
////                    match b with
////                    | bhead :: btail -> source :: [mingle tail btail ]
////            mingle s1 s2
//
//        let stringMingling2 first:string second : string =
//            let result = first |> Seq.mapi (fun i f -> String.Concat([f; second.[i].ToString()])) |> fun r -> String.Join("", r)
//            result
//
//        printfn "%s" (stringMingling2 "hacker" "ranker")

//        printfn "%s" (stringMingling "hacker" "ranker" "")
